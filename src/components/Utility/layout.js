import React from "react";
import { Navbar } from "react-bootstrap";

const layout = ({ children }) => {
  return (
  <>
    <Navbar></Navbar>
    <footer></footer>
  </>
  );
};

export default layout;
